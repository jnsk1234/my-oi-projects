#include<bits/stdc++.h>
using namespace std;

cmp(string a,string b)
{
	if(a.size()!=b.size())
	{
		return a.size()<b,size()
	}
	return a>b;
}
int n;
int main()
{
	cin>>n;
	for(int i=1;i<=n;i++)
	{
		cin>>s;
		if(cmp(name,s))
		{ 
			name=s;
			id=i;
		}
	}	
	cout<<id<<endl<<name;
    return 0;
}

